

ctrl + alt + t - октрытие терминала. 
ls - просмотр содержимого текущей директории. 
ls - путь до директории. Просмотрт содержимого указанной директории. 
ls -a  просмотр скрытых файлов директории.
mkdir - Название директории/путь. создание директории.
cd - путь до директории, смена директории.
pwd - вывод абсолютного пути в консоль.
. - ссылка на текущую директорию. 
.. - ссылка на директорию выше либо родительскую папку. 
~ - ссылка на домашнюю директорию. 
cat - получение/перемещение текста. 
nano - путь для открытия файла. Открытие текстового редактора nano, если файл не существует - создает его. 
mv - путь до файла, путь до директории. - перенос директории/файла в указанную директорию. 
mv путь до файла новое название переименование файла/директории.
echo - какой-то текст - вывод указанного текста в консоль.
echo some_text > new_file - запись/перезапись текста в файл
echo appended_text >> same_file - дозапись текста в файл.
rm - удаление файлов
rmdir - удаление директорий.
rm -r - рекурсивно удалить содержимое директории и её саму.
rm -f - принудительное удаление.
rm -rf
echo слово куда.
sudo apt update - обновление.
Абсолютный путь - путь, который начинается с корневой директории(/home/username/.....)

Относительный путь - путь, относительно других директорий(../some_dir)
ls -al вывод памяти.
printenv - вывод переменных окружения.
Ctrl + C - остановка определенного процесса.
ps - команда для вывода запущенных процессов.
!! - использование последней введенной команды.
export - добавление переменной в переменную окружения.


